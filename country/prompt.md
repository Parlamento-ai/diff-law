# Prompt template para deep research por país

Este es el prompt base. Reemplazar `{COUNTRY}` y `{LOCAL_TERM}` por el país y término local correspondiente.

---

El prompt específico de cada país está en `country/{pais}/prompt.md`.
